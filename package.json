{
  "name": "rtl-for-vs-code-and-cursor-agents",
  "displayName": "RTL for VS Code & Cursor Agents",
  "description": "Right-to-left (RTL) support for AI chat agents: Cursor, Claude Code, Codex (ChatGPT), Copilot, Gemini, Antigravity.",
  "version": "1.0.1",
  "publisher": "udah1",
  "license": "GPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/udah1/rtl-for-vs-code-and-cursor-agents.git"
  },
  "icon": "icon128.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "main": "./src/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "commands": [
      {
        "command": "rtlForVsCodeAgents.checkAndInject",
        "title": "RTL for VS Code Agents: Check and Inject Claude Code"
      },
      {
        "command": "rtlForVsCodeAgents.configureCustomCss",
        "title": "RTL for VS Code Agents: Configure Custom CSS Loader"
      },
      {
        "command": "rtlForVsCodeAgents.checkForUpdates",
        "title": "RTL for VS Code Agents: Check for Updates"
      },
      {
        "command": "rtlForVsCodeAgents.removeInjections",
        "title": "RTL for VS Code Agents: Remove All RTL Injections"
      }
    ],
    "configuration": {
      "title": "RTL for VS Code Agents",
      "properties": {
        "rtlForVsCodeAgents.autoInject": {
          "type": "boolean",
          "default": true,
          "description": "Automatically check for new Claude Code versions and inject RTL support."
        },
        "rtlForVsCodeAgents.checkIntervalHours": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "How often to check for new Claude Code versions (in hours). 0 disables periodic checks (startup only)."
        },
        "rtlForVsCodeAgents.autoConfigureCustomCss": {
          "type": "boolean",
          "default": false,
          "description": "Automatically configure Custom CSS and JS Loader to load the RTL script."
        },
        "rtlForVsCodeAgents.userMessageBorder": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Show a colored border around user messages for easy identification.\n\n**Tip:** It's easier to toggle this by right-clicking the ↑↓ navigation buttons — changes are instant (no reload needed). This setting is only used as the initial default on first install."
        },
        "rtlForVsCodeAgents.yoloCountdownSeconds": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "maximum": 30,
          "markdownDescription": "YOLO mode countdown duration (seconds) before auto-approving tool calls.\n\n**0 = instant** (no progress bar).\n\n**Tip:** It's easier to change this by right-clicking the 💪 button directly — changes are instant and persistent (no reload needed). This setting is only used as the initial default on first install."
        },
        "rtlForVsCodeAgents.monacoRtlEnabled": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Enable RTL support for Monaco Editor input boxes (e.g. Copilot chat input).\n\n**Default is off** because right-aligning Hebrew/Arabic inside Monaco editors also affects regular code editor windows — the text caret doesn't move correctly through RTL text and text selection misbehaves. Chat messages still get RTL even when this is off. Enable only if you specifically want Copilot's Monaco-based chat input to flip direction.\n\n**Tip:** Changes take effect after running 'Disable Custom CSS and JS' → 'Enable Custom CSS and JS' → reload."
        },
        "rtlForVsCodeAgents.autoCheckUpdates": {
          "type": "boolean",
          "default": true,
          "description": "Automatically check for updates from GitHub on startup."
        },
        "rtlForVsCodeAgents.updateCheckIntervalHours": {
          "type": "number",
          "default": 24,
          "minimum": 0,
          "description": "How often to check for updates (in hours). 0 = check on startup only."
        }
      }
    }
  }
}
